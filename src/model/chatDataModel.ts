interface ChatDataModelProps {
  name: string;
  avatarImg: string;
  lastMessageAbstract: string;
  lastMessageTime: number;
  newMessagesCount: number;
}

export default function formChatsData(data: ChatDataModelProps) {}
